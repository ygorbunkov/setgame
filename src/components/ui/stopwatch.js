import React from 'react'
import { Typography } from '@material-ui/core'

const Stopwatch = () => (
	<Typography variant="h3" style={{width:'50%', margin:'auto', textAlign: 'center'}}> 0s </Typography>
)

export default Stopwatch